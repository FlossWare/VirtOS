# test_helper.bash
# Common test helper functions for BATS tests

# This file provides shared utilities for VirtOS BATS tests.
# Currently minimal, but can be extended with common functions.

# Example helper function (currently unused, but demonstrates structure)
# check_command_exists() {
#     command -v "$1" >/dev/null 2>&1
# }
